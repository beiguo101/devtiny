# macOS Package

This directory contains prebuilt DevTiny packages for macOS.

## Current Package

```text
DevTiny_0.1.0_aarch64.dmg
```

This package is built for Apple Silicon Macs.

Docker features require Docker Desktop to be installed and running. Git features
require Git to be installed on the local machine.
